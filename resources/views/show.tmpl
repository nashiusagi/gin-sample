<!DOCTYPE html>
<html>
    <head>
        <title>{{.title}}</title>
    </head>
    <body>
        <h1> Blog </h1>
        {{range $index,$element := .posts}}
                <h2>{{ $element.Title }}</h2>
                <p>{{ $element.Body }}</p>
        {{end}}
    </body>
</html>
